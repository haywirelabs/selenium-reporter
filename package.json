{
  "name": "selenium-reporter",
  "version": "0.0.0",
  "description": "Node.js application to run Selenium tests via the selenium-standalone jar and report in XML for consumption in JUnit",
  "main": "bin/selenium-reporter.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joe Osborne",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.cms.gov/CMSGov/selenium-reporter"
  },
  "dependencies": {
    "selenium-webdriver": "2.41.0",
    "graceful-fs": "2.0.3",
    "mocha": "1.18.2",
    "mocha-bamboo-reporter": "*",
    "chai": "1.9.1",
    "argparse": "0.1.15"
  },
  "scripts": {
    "bamboo": "node node_modules/mocha/bin/mocha -R mocha-bamboo-reporter"
  }
}
